class Solution {
    int print2largest(int arr[], int n) {
        // code here
         Arrays.sort(arr);

        int sec=arr[arr.length-1];

        for(int i=arr.length-1;i>=0;i--){

               if(arr[i]<sec)

               return arr[i];

        }

        return -1;
    }
}
